'use client'

// Wrapper: editor soal UTS/UAS untuk GURU (pemilik scope) — komponen yang sama
// dipakai admin di /dashboard/admin/uts-uas/[id]. Link internal sudah role-aware.
export { default } from '@/app/dashboard/admin/uts-uas/[id]/page'
